import type { Metadata } from "next";
import { Geist, Geist_Mono, Inter } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
});

export const metadata: Metadata = {
  title: "Alexandre | Front-End Developer",
  description: "Portfolio of Alexandre, a Front-End Developer passionate about building beautiful, functional, and intuitive digital experiences.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${inter.variable} ${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>

      <footer className="bg-[#323a56] h-14 text-white font-bold text-md items-center flex justify-center text-center">Copyright ©2025 Alexandre. Designed by Dev Alexandre</footer>
    </html>
  );
}
